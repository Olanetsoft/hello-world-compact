{
  "name": "hello-world-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compact": "compact compile src/hello.compact src/managed/hello",
    "build": "rm -rf dist && tsc && cp -Rf ./src/managed ./dist/managed"
  }
}
