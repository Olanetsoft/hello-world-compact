{
  "name": "@midnight-ntwrk/hello-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "start": "docker compose -f proof-server.yml up -d && node --experimental-specifier-resolution=node --loader ts-node/esm src/cli.ts",
    "stop": "docker compose -f proof-server.yml down"
  },
  "dependencies": {
    "@midnight-ntwrk/hello-contract": "*"
  }
}
